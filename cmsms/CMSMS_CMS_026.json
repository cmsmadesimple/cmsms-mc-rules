{
  "rule_id": "CMSMS_CMS_026",
  "title": "Uninstall does not clean up resources created by install",
  "category": "cmsms",
  "severity": "high",
  "confidence": "high",
  "score": 6,
  "description": "The method.uninstall.php file does not remove resources that method.install.php creates. If install creates permissions, uninstall must remove them. If install creates tables, uninstall must drop them. If install creates preferences, uninstall should remove them. If install creates events, uninstall should remove them.",
  "detection": {
    "type": "heuristic",
    "patterns": []
  },
  "context": [
    "lifecycle",
    "cleanup"
  ],
  "requires": [
    "install_uninstall_cross_check"
  ],
  "applies_to": [
    "module"
  ],
  "explanation": "A module that creates database tables, permissions, preferences, or events during install must clean them up on uninstall. Orphaned data wastes space, orphaned permissions clutter the admin UI, and orphaned tables can cause conflicts if the module is reinstalled or another module reuses the name.",
  "fix": "For every CreatePermission() in install, add RemovePermission() in uninstall. For every CreateTableSQL(), add DropTableSQL(). For every SetPreference(), add RemovePreference(). For every CreateEvent(), add RemoveEvent().",
  "examples": {
    "bad": "// method.install.php\n$this->CreatePermission('Manage MyModule');\n// method.uninstall.php\n// (empty - permissions left behind)",
    "good": "// method.install.php\n$this->CreatePermission('Manage MyModule');\n// method.uninstall.php\n$this->RemovePermission('Manage MyModule');"
  },
  "tags": [
    "lifecycle",
    "cleanup",
    "install",
    "uninstall"
  ],
  "version": "1.0",
  "enabled": true
}
