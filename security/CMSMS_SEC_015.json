{
  "rule_id": "CMSMS_SEC_015",
  "title": "User input decoded from base64 then used in dangerous sink",
  "category": "security",
  "severity": "high",
  "confidence": "high",
  "score": 9,
  "description": "base64_decode() is called on user input and the result flows into echo, SQL, include, or eval.",
  "detection": {
    "type": "heuristic",
    "patterns": []
  },
  "context": [
    "encoding",
    "output",
    "sql"
  ],
  "requires": [
    "user_input_flow",
    "decode_before_sink"
  ],
  "applies_to": [
    "php"
  ],
  "explanation": "Base64-decoding user input and passing the result to a dangerous sink bypasses WAFs and input filters.",
  "fix": "Validate and sanitize the decoded content before use.",
  "examples": {
    "bad": "$html = base64_decode($_POST['content']);\necho $html;",
    "good": "$raw = base64_decode($params['content'] ?? '', true);\nif ($raw === false) { return; }\necho htmlspecialchars($raw, ENT_QUOTES, 'UTF-8');"
  },
  "tags": [
    "encoding",
    "bypass",
    "xss",
    "input"
  ],
  "version": "1.0",
  "enabled": true
}
